name: TMP descoberta de colunas (remover)
on:
  push:
    branches: [ "claude/vibrant-hamilton-1ex0x5" ]
    paths: [ ".github/workflows/descoberta-tmp.yml", ".github/descoberta.py" ]
permissions:
  contents: read
jobs:
  headers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: python3 .github/descoberta.py
